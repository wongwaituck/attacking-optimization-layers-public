# Setting the All Zero Matrix as the Target

First place the script into the folder containing trained Jigsaw Sudoku models. Then, run the 
following script:

```
./attack_zero.py --loss-function=ZeroMatrixLoss
```